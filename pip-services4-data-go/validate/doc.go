// Validation frameworks exist in various languages,
// but since this is one of the underlying functions that
// is incorporated into (various) other packages,
// we decided to implement it in a portable format,
// for identical implementation across languages.

package validate
